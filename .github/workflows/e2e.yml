name: e2e cypress

on: [push]

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v2

      - uses: cypress-io/github-action@v2

        with:
          build: npm run build
          start: npm start
